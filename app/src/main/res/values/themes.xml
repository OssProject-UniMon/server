<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Why1" parent="Theme.AppCompat.Light">
        <!-- Customize your light theme here. -->
        <item name="windowNoTitle">true</item>
        <item name ="colorPrimaryVariant">@color/mainColor</item> <!--위의 상태창 색깔변경 -->
    </style>

    <style name="Theme.Why1" parent="Base.Theme.Why1" />

    <style name="AuthEditText" parent="android:Widget.EditText">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textSize">20sp</item>
        <item name="android:layout_margin">10dp</item>
    </style>
</resources>